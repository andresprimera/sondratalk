- [x] Quitar cursivas (sobre todo en Sondra) — removed `italic` from the Sondra wordmark (sidebar, header, landing, onboarding logos) and all app-source uses. Proof: `screenshots/italics-dashboard-sidebar.png`
- [x] la mini encuesta post conversación (yo hice el mockup) - /Users/andresprimera/apps/personal/sondratalk/post-conversation-mockup.html — built the post-conversation wrap-up: new `feedback` backend module (upserts one feedback doc per meeting+user, validates participation) + shared schema + `/dashboard`-styled wrap-up page at `/call/:meetingId/wrap-up` (talk-again, leave-the-door-open note, feeling, circle/contact/AV quick questions, private star rating, private notes, and a report-an-issue dialog). The call's "End call" now routes here. en/es translated. Proof: `screenshots/wrapup-form.png` (filled form), `screenshots/wrapup-done.png` (confirmation).
- [x] que el dashboard cuente las conversaciones de verdad, y el mes en que entró el usuario. Lo de host exp. creo que todavía no aplica en Beta — added `GET /api/meetings/stats` (counts past, non-cancelled meetings), exposed `createdAt` on the User contract for "Active since", and dropped the Host Exp stat for Beta. Proof: `screenshots/dashboard-real-counts.png` (0 conversations, "May 2026", no Host Exp).
- [x] Modificar el landing -> /Users/andresprimera/apps/personal/sondratalk/landing-full-mockup.html — rebuilt landing.tsx to match the full mockup (hero, "What it is", "How it works" steps, "Who it's for", manifesto close, footer tagline), theme-aware light/dark, all strings in en/es. Proof: `screenshots/landing-rework.png` (light), `screenshots/landing-rework-dark.png` (dark).
- [x] cuando agrandaste la letra se desconfiguró el layout en el onboarding, hay que ensancharle el espacio para que se vea como antes — widened the onboarding container (`max-w-[520px]` → `max-w-[600px]`) and fixed the languages step: the fluency toggle group had `w-fit` + `flex-wrap: wrap`, which collapsed the pills onto 3 lines. Set the group to `width: max-content` / `flex-wrap: nowrap` so "Conversational · Fluent · Native" stays on one row, with a `≤32rem` fallback that stacks the row. Proof: `screenshots/onboarding-step2-before.png` (pills wrapped), `screenshots/onboarding-step2-after.png` (single row).
- [x] cuando hace clic en guardar franjas, vuelva al dashboard (panel) — `handleSave` in `availability.tsx` now `navigate("/dashboard")` on mutation success (after the "Saved" toast). Proof: `screenshots/availability-save-redirect.png` (lands on the dashboard, "Saved" toast visible, the toggled "Weekday mornings" window reflected).
- [x] cuando agendas la conversación; que vaya directo al panel, no? — `confirmSelectedSlot` in `find-conversation.tsx` now `navigate("/dashboard")` after a scheduled slot is confirmed (both the simulated/mock branch and the real `createMeeting` success), instead of resetting back to the request form. "Talk Now" still routes to the live call. Proof: `screenshots/schedule-redirect.png` (lands on the dashboard with the "We'll let them know — you'll see it in your dashboard" toast).
- [x] Debería estar arriba "Activo desde" y abajo "may 2026" (que no se traduce, btw). Conversaciones puede quedar así, o por diseño, también ponerlo arriba, como título — flipped `StatsStrip` so the label sits on top as a title and the value below (applied to both stats for consistency, per the design note). Fixed the date "not translating": it used `month: "short"`, whose Spanish form ("may 2026") is indistinguishable from English — switched to `month: "long"` so it renders "mayo de 2026" in es / "May 2026" in en. Proof: `screenshots/stats-en.png`, `screenshots/stats-es.png`.
- [x] si les es muy dificil a los usuarios cuadrar horario, se podría pensar en un chat restringido con opciones, pa que no puedan hablar pero puedan poner que si "9.30am?" --"sí" — built the restricted, option-only scheduling thread (full feature). Shared `scheduling` schemas; backend `SchedulingMessage` model + `SchedulingService`/`SchedulingController` nested at `/api/meetings/:meetingId/scheduling` (GET thread, POST proposals, POST responses), with `MeetingsService.reschedule` moving the meeting + re-sending invites when a proposal is accepted. Frontend `/dashboard/conversations/:meetingId/schedule` page: no free-text — you pick a day+time from chips to propose ("Mon, Jun 1, 9:00 AM?"), the peer answers Yes/No, and Yes confirms + reschedules. Reachable via a "Propose a time" button on each scheduled upcoming conversation. en/es, loading/error/empty states, backend + frontend unit tests. Proof: `screenshots/scheduling-proposed.png` (Ana proposes), `screenshots/scheduling-confirmed.png` (Marta accepts → "Currently scheduled for" moves to the agreed slot, "Confirmed" badge + toast).
- [x] el botón "Hablar ahora" no funciona (ni en offline ni online). — wired up the Talk Now button on the dashboard: if offline, shows a `toast.info` ("You're offline for Talk Now") with a "Go online" action that enables availability then navigates; if online, navigates to `/dashboard/find-conversation?mode=talknow` which pre-selects "talk" intent and auto-starts the match search once circles load (via `useSearchParams` + `useRef` auto-start guard). Proof: `screenshots/talk-now-offline-toast.png` (offline toast with action)
- [x] ELIMINAR LA OPCIÓN DE "Simular una coincidencia (previsualización)" — removed `simulateMatch` function, `SimulatedMatch` type, `simulatedMatch` state, and the preview button from `find-conversation.tsx`; also removed both i18n keys; mock cards now have `talkNowDisabled` so the Talk Now button is visually disabled for non-real IDs. Proof: `screenshots/find-conversation-no-simulate.png` (page with no simulate button)
- [x] "Not really - just talk" option should be the first one. AND, the other two options should say "Not available in Beta version" or something like that. — reordered intents (talk first, then specific, then heard); "specific" and "heard" now show "Not available in Beta yet." hint text below them; `startSearch()` fires a `toast.info` for those two instead of searching; removed dead heard/specific UI expansions, mock pool, suggestion engine, and `useFindHeardMatch` hook. Proof: `screenshots/intent-order-beta.png` (new order + hints), `screenshots/intent-beta-toast.png` (toast on click)
- [x] The user has to know they will be able to propose another time for the meeting before they pick a time. — added helper note "Not seeing a good time? You can propose another time after scheduling." below the "Next available" label in the slot picker on the matches stage. en/es translated. Proof: `screenshots/slot-propose-note.png`
- [x] Change "sign up" buttons or links for "Apply" — renamed "Sign up" link in login form, "Join Sondra" (×3) on landing, and "Create Account" submit button in signup form all to "Apply"; removed stale locale keys; updated e2e test selectors. Proof: `screenshots/apply-landing.png`, `screenshots/apply-signup.png`
- [x] The onboarding should include a short form. A 48-hour response. This signals: we are selective, this is intentional, your presence here matters. Even if you accept 95% of applicants, the application creates commitment. — added step 4 "application" to onboarding (textarea: "Why are you looking for these conversations?", 1000-char limit, "We'll review within 48 hours" hint); welcome step updated to "Your application is in" + 48-hour review message; backend PATCH me/application endpoint + shared schema + service method + tests; welcome is now step 5, progress bar shows 4 steps. en/es translated. Proof: `screenshots/onboarding-application-step.png`, `screenshots/onboarding-welcome-48h.png`
- [x] The counter on the homepage: "147 founding members. Applications open." Never show "users online now." — added public GET /api/users/count endpoint; landing page fetches and displays "N founding members · Applications open" below hero CTAs (hidden while loading/erroring, never shows online-now). en/es translated. Proof: `screenshots/founding-members-counter.png`
- [x] Circles dropdown show the id on selection and not the actual value. (It would be good to check if everything other dropdown is correct) — root cause: this project's Select is Base UI (not Radix), whose `Select.Value` renders the raw selected value unless `Select.Root` gets an `items` prop mapping value→label. Added `items` to the theme Selects in `add-circle-dialog`, `edit-circle-dialog`, and the `circles.tsx` theme filter (were showing the theme UUID) and to the `add-user-dialog` role Select (was showing untranslated `user`/`admin`). Audited the rest: page-size Selects and the `users.tsx` inline role Select have value === label, so they were already correct. Proof: `screenshots/circles-dropdown-label.png` (theme filter shows "Dogs"), `screenshots/circles-dropdown-add-dialog.png` (Add Circle dialog shows "Dogs").
- [x] Circles: comma separated input doesn't let you include commas. It's worth it to confirm that it actually correctly uses the comma separated values downstream. — root cause: the alias inputs bound `value` straight to `field.value.join(", ")`, so the parsed `.filter(Boolean)`'d array round-tripped back on every keystroke and stripped a separator comma the instant you typed it. Extracted a reusable `CommaSeparatedInput` that keeps the raw typed text in local state (commas + trailing spaces survive) while still emitting the parsed `string[]` via `onChange`; wired it into the en/es aliases fields of both the add and edit circle dialogs. Downstream confirmed correct: aliases are stored as `aliases.{locale}` string arrays and fed to Atlas Search autocomplete in `circles.service.ts` — each alias is a separate searchable entry, so the comma-separated values are used as intended. Added 5 unit tests for the component. Proof: `screenshots/circles-alias-comma-input.png` (Add Circle dialog showing "GSD, Alsatian, German Shepherd" typed with commas intact).
- [x] the early access list is supposed to prevent any user from dreating an account, but it's not working. I could create an account (at least the first step with user name, email and password). This should not be allowed and an explicit error message should be given to the user — root cause: `AllowlistService.isEmailAllowed` returned `true` whenever the allowlist collection was empty (a bootstrap hatch), so once any user existed the gate never closed and any email could sign up. Made `isEmailAllowed` a strict membership check (exact email or `@domain`) and moved the first-user bootstrap exemption into `AuthService.signup` (keyed on `countUsers() === 0` → that user becomes admin and skips the gate); every later signup must be allowlisted, so an empty allowlist now blocks everyone. Signup now 403s with an explicit, translated message ("This email isn't on the early access list. Sondra is invite-only right now.") which the form surfaces as a toast; updated en/es keys and backend tests (strict allowlist + bootstrap-exempt + not-allowlisted-reject). Proof: `screenshots/early-access-signup-blocked.png` (403 toast, stays on /signup).
- [x] the sidebar doesn't seams to correctly highlight current route selection. — root cause: the nav buttons never set `isActive`, so `SidebarMenuButton`'s `data-active:bg-sidebar-accent` highlight never applied; `nav-secondary` also used a raw `<a href>` (full page reload). Added an `isNavActive(pathname, url, { end })` helper, computed the active state from `useLocation()` in both `nav-main` and `nav-secondary`, switched `nav-secondary` to `<Link>`, and marked the `/dashboard` root item `end: true` so it exact-matches (the prefix guard also stops `/dashboard/circles-extra` lighting up `/dashboard/circles`). Unit tests for the helper + an e2e regression spec. Proof: `screenshots/sidebar-active-dashboard.png` (Dashboard lit on the index), `screenshots/sidebar-active-settings.png` (Settings lit, Dashboard de-highlighted on the child route).
- [x] change the invite link to just "www.sondratalk.com" — replaced the user-specific slug constant with a single inline `"https://www.sondratalk.com"` on the `CopyableInput`; removed the now-redundant `referralUrl` module-level const and the `copyValue` split. Proof: `screenshots/invite-link.png`
- [x] use the same favicon and browser tab title in beta.sondratalk.com as in sondratalk.com — fixed `<title>` from the Vite scaffold placeholder "frontend" to "Sondra" in `index.html`; favicon was already the correct Sondra purple bolt SVG. Proof: `screenshots/tab-title-favicon.png`
- [x] implement this mockup to confirmation email /Users/andresprimera/apps/personal/sondratalk/email-confirmation-mockup.html — rebuilt the scheduled-conversation confirmation email (`meeting-email.ts`) to match the mockup: dark card + green stripe, "Conversation confirmed" header, headline naming the peer, a time block (day · time range · timezone), a green "Join Conversation" CTA, join note, and the Sondra / sondratalk.com footer. Email-client-safe (table layout, inline styles, no `<style>`/flexbox/web-font dependency), HTML-escaped peer name. Split the combined date into `formatDayLabel`/`formatTimeRange`, changed the subject to "Your conversation with X is confirmed", en/es throughout, updated specs (incl. an XSS-escaping test). Timezone still shows UTC — task 27 converts it to the user's local time. Proof: `screenshots/email-confirmation-en.png` (en), `screenshots/email-confirmation-es.png` (es).
- [x] el default de beta.sondratalk.com es dark, bien, pero no cuando se cliquea desde sondratalk.com — root cause: this is a client-only Vite SPA, so next-themes only adds `class="dark"` to `<html>` after the JS bundle mounts; on a cold load (clicking into beta from sondratalk.com) the page first painted the light `:root` theme, then snapped to dark. Added a tiny blocking inline bootstrap `<script>` in `index.html` that sets the `dark` class before first paint, reading next-themes' `theme` localStorage key and defaulting to dark (only "light" opts out) — mirrors the `ThemeProvider` config. Added an e2e regression spec that blocks `/src/main.tsx` to prove the inline script (not React) applies the theme. Proof: `screenshots/theme-default-dark.png` (cold load, empty localStorage → `<html class="dark">`).
- [x] Let's widen the max width of the container on the logged in user section a little bit. — bumped the user-section content column from `max-w-2xl` (42rem) to `max-w-3xl` (48rem) across every logged-in user page: dashboard, availability, find-conversation (all three stages), conversation-scheduling, my-circles, and settings. With the 19px root font-size that's ~798px → ~912px. Left the admin pages (full-width tables) and the call/wrap-up full-screen flows untouched. Proof: `screenshots/dashboard-width-before.png` (798px column), `screenshots/dashboard-width-after.png` (912px column).
- [x] Font size is too small. Let's proportionally increment font size across the site. — the whole UI is rem-based off a single `--font-size-base` token (`html { font-size: var(--font-size-base) }`), so bumping it from 19px to 21px (~+10.5%) proportionally enlarges every page at once. Verified no layout regressions on the landing, dashboard, and — critically — the onboarding languages step whose fluency pills broke on the last font bump (they still hold on one row at 21px). Proof: `screenshots/font-size-landing-before.png` vs `font-size-landing-after.png`, `font-size-dashboard-before.png` vs `font-size-dashboard-after.png`, and `font-size-onboarding-languages.png` (pills intact at 21px).
- [x] el mail envia la hora en UTC, debería de enviarla en la hora del usuario — the confirmation email now renders the conversation time in each recipient's own timezone (from their `user.timezone`, set at onboarding / in settings) instead of UTC. Gave `formatDayLabel`/`formatTimeRange` a `timeZone` param, added `formatTimeZoneLabel` (DST-aware short label like "GMT-4" / "CEST" off the conversation date), and append a localized "your local time" / "tu hora local" suffix. The service passes `recipient.timezone` per email, so the two participants can each see their own local time. The ICS attachment stays in UTC (calendar apps localize it). Backend + service tests for the shift. Proof: `screenshots/email-localtime-newyork.png` (10:30 GMT-4 · your local time) and `screenshots/email-localtime-madrid.png` (16:30 CEST · tu hora local) — same 14:30 UTC conversation.
- [x] Include, in admin dashboard, a place where i can see the answer to the onboarding question. it would be nice if it also can be sent to raul.harlev@gmail.com — added `applicationText` to the shared `User` schema and `toUser` mapper; new `/dashboard/applications` admin page (table: Name, Email, Joined, Answer) backed by the existing paginated users endpoint; notification email sent to `ADMIN_NOTIFICATION_EMAIL` (env var) on each application submission, fire-and-forget so it never blocks the response. Proof: `screenshots/admin-applications.png`
- [x] include, in admin dashboard, a place where i can see the post conveersation survey answers; you decide the structure of it, i.e., name, conversation number of that user, answers. — added `AdminFeedback` to shared; `GET /api/feedback` admin-only endpoint using a `$setWindowFields` aggregate (per-user conversation index) + `$lookup` for user name/email; new `/dashboard/survey-answers` page (table: Name, Conv. #, Talk again, Circles relevant, AV quality, Rating, Feeling, Notes, Report, Date); unit tests for `findAllForAdmin` and `fetchAdminFeedbackApi`. Proof: `screenshots/admin-survey-answers.png`
- [x] in "users" in the backend, include the following columns: checkbox so I can select several, number of conversations, host exp points, active since; also, can you add another option in the "role" dropdown? --> User. Admin. Founding Member. Also it'd be nice to be able to organize alphabetically by name or role (sort column option) — added checkbox multi-select (select-all + per-row) with bulk delete, Conversations column (via MongoDB `$lookup` aggregate on meetings), Host Exp column, Active since column; added "Founding Member" role across shared schema + Mongoose + all dropdowns; clickable Name/Role column headers with asc/desc sort (server-side via `$sort` in aggregate). Proof: `screenshots/task-30-users-admin.png`, `screenshots/task-30-users-sorted.png`, `screenshots/task-30-users-role-labels.png`
- [x] in backend - circles menu: Delete columnd "Actions". Change current "popularity" header to "Circle Type". Create a new column "Popularity" that counts how many users (or admin or founding members) have picked that circle. Also include the posibility to sort alphabeticaly the columns. — removed the Edit/Delete Actions column (and the now-dead EditCircleDialog + updateCircleApi/removeCircleApi); fixed the pre-existing header/cell mismatch bug by renaming the mislabeled "Popularity" header to "Circle Type"; added a genuine "Popularity" column showing a live membership count via a MongoDB `$lookup` aggregate against `circle_memberships`; all 6 columns are sortable (server-side `$sort`, with column sort taking priority over relevance during text search). New admin-only `GET /api/circles/admin` endpoint. Proof: `screenshots/task-31-circles-admin-table.png`
- [x] in the "+Add circle" pop-up, remove popularity field. — removed the manual "Popularity" number input from `AddCircleDialog`; the field stays optional in the shared schema and defaults to 0 server-side. Proof: `screenshots/task-32-add-circle-no-popularity.png`
- [x] la propuesta de nuevo horario no está en producción, ni avisa ni manda mail. Can you fix this? — the scheduling-proposal flow (`/dashboard/conversations/:meetingId/schedule`) was wired up but silent: proposing a new time or declining a peer's proposal never notified the other person. Added two fire-and-forget email notifications (mirroring the existing booking-confirmation email pattern): the peer gets emailed when a new time is proposed, and the original proposer gets emailed if their proposal is declined. Accepting a proposal already emailed both sides via the existing `MeetingsService.reschedule()` calendar-invite resend — that path is untouched. en/es copy, backend unit tests. No UI proof (backend/internal email-notification change; behavior verified via unit tests covering propose/decline/accept email branches).
- [x] When on a call, if the user is using their phone (responsive), "my" face is on top of the other user face, even though there is enough vertical space. We should address this. — made the call video layout responsive. The stage previously forced a single 16:9 box with the local feed as a bottom-right picture-in-picture overlaying the remote face, so on a portrait phone (a short, vertically-centered box) "my" face sat on top of the other person while vertical space went unused. Now on phones/portrait (`<640px`) the two feeds stack as two equal tiles that fill the available vertical space — no overlap; on `sm:`+ the original immersive remote-fills-stage + small corner PiP is preserved unchanged. Extracted shared `remoteTileClass`/`localTileClass` Tailwind constants consumed by both the live `VideoStage` and `ConnectingPlaceholder`, plus an e2e regression spec asserting the stacking geometry. Proof: `screenshots/responsive-call-mobile-stacked.png` (mobile, stacked), `screenshots/responsive-call-desktop-pip.png` (desktop, corner PiP).
- [x] When on a call, if the user is using their phone (responsive), "my" face is on top of the other user face, even though there is enough vertical space. We should address this. — made the call video layout responsive. The stage previously forced a single 16:9 box with the local feed as a bottom-right picture-in-picture overlaying the remote face, so on a portrait phone (a short, vertically-centered box) "my" face sat on top of the other person while vertical space went unused. Now on phones/portrait (`<640px`) the two feeds stack as two equal tiles that fill the available vertical space — no overlap; on `sm:`+ the original immersive remote-fills-stage + small corner PiP is preserved unchanged. Extracted shared `remoteTileClass`/`localTileClass` Tailwind constants consumed by both the live `VideoStage` and `ConnectingPlaceholder`, plus an e2e regression spec asserting the stacking geometry. Proof: `screenshots/responsive-call-mobile-stacked.png` (mobile, stacked), `screenshots/responsive-call-desktop-pip.png` (desktop, corner PiP).
- [x] Match Anonymity — Fix: Matches currently show real names. Fix:
      Default: anonymous. Match A, Match B + circles as chips + italic intent line. No names, ever, by default — including rematches with someone talked to before.
      Exception: if both users have mutually "left the door open" (already implemented), show the real name instead of the anonymous label. No new UI section needed — just reveal the name where the match would otherwise appear.
      New: "don't match again" — one-directional, no notice to other party, persists until reversed, overrides everything including mutual door-open. Standalone exclusion table, not folded into the door-open data.
      Done: candidate cards in find-conversation.tsx now show "Match A"/"Match B"/etc. (by list position) with an italic intent line instead of the real first name; the call, wrap-up, and upcoming-meetings screens fall back to the existing "Your match" placeholder. Added `MeetingsService.hasMutualDoorOpen()`, which checks every past meeting between a pair for a mutual `doorOpen`, consumed by both `MeetingsService` (call/wrap-up/dashboard) and `MatchingService` (candidate list) to decide whether to reveal the real name — the reveal is permanent once it happens. Added a standalone `MatchExclusionsModule` (`match_exclusions` collection, one-directional `fromUserId`→`toUserId`) wired into `FeedbackService.upsert()` via a new `dontMatchAgain` toggle on the wrap-up page (next to "Leave the door open") and into `MatchingService.collectCandidates()` to filter excluded users out of future searches. Added backend unit tests covering anonymization, mutual reveal, and exclusion filtering across 4 spec files. No UI proof — this sandbox can't reach the project's MongoDB Atlas cluster (IP not whitelisted), so the live app couldn't be run here; verified instead via passing lint/typecheck/build and the full test suite (267 backend, 108 frontend) plus a clean code review.
- [x] Private Circles — gate a circle behind a password so non-admin users must enter it before joining.
      Data model: add two fields to the Circle entity (shared `circleSchema`/`createCircleSchema`/`updateCircleSchema` in shared/src/schemas/circle.ts + backend/src/circles/schemas/circle.schema.ts): `isPrivate` (boolean, default false) and `password` (string, optional). Keep field names camelCase per convention — admin table columns can be labeled "Private" / "Password", but don't name the underlying fields after the display labels.
      Security: hash `password` (bcrypt, same as user passwords elsewhere in this app) rather than storing plaintext, and mark it `select: false` on the Mongoose schema like other sensitive fields. General-purpose circle read responses (used by `/circles`, `/circles/all`, onboarding, my-circles) should only ever expose `isPrivate` — never the password or its hash.
      Backend: add a join-time check (e.g. `POST /api/circles/:id/verify-password`) that compares a submitted password to the stored hash and returns pass/fail only. Password is write-only through the API — set on create, never returned afterward. Open question: do admins need a way to see/reset an existing circle's password later?
      Frontend — admin: update `add-circle-dialog.tsx` with a "Private" yes/no control and a "Password" text field that only renders when Private = yes; extend the create-circle Zod schema with a refinement requiring a password when `isPrivate` is true. Note: circles have no edit flow today (create-only), so a private circle's password/flag can't be changed later until an edit dialog exists — flag if that's also needed.
      Frontend — end users: in `onboarding-circles-step.tsx` and `my-circles.tsx`, when a user clicks an `isPrivate` circle not already in their selection, show a password-prompt dialog (same Dialog pattern used elsewhere) before adding it; verify via the new endpoint and only commit the selection on success, with a clear inline error on a wrong password.
      Done: per user decision, no edit-circle flow was built — Private/Password can only be set at creation, in `add-circle-dialog.tsx` (a "Private" Yes/No select that reveals a `PasswordInput`). Backend hashes with bcrypt (`select: false`), exposes only `isPrivate` to readers (both admin aggregation pipelines explicitly project out `password`), and gates joining via `POST /circles/:id/verify-password` (403 on a wrong password — deliberately not 401, since `authFetch` treats any 401 as session-expiry and would trigger a needless silent token refresh). `CirclesService.update()` (the existing PATCH endpoint, unused by any UI yet) re-validates the same isPrivate/password invariant against the stored document, not just the payload, and clears the hash if a circle is ever un-privated. New shared `CirclePasswordDialog` (react-hook-form + Zod, matching the project's form convention) is used by both `onboarding-circles-step.tsx` and `my-circles.tsx` to intercept selecting a private circle. Extensive new backend unit tests (hashing, verification, exclusion of `password` from aggregations, the update-time consistency check, the $set/$unset un-privating edge case). No UI proof — same sandbox limitation as the Match Anonymity task above (MongoDB Atlas unreachable); verified via passing lint/typecheck/build and the full test suite (279 backend, 109 frontend) plus 5 rounds of code review until clean.

## 2026-06-24

- [x] Time on confirmation emails is in UTC. I think this is confusing to users. We should show each user timezone on the emails. — Root cause: the email code already renders times in `recipient.timezone`, but accounts created before signup captured a timezone (and the bootstrap/seeded admin) keep the schema default `"UTC"`, so their emails fall back to UTC. Added `useTimezoneSync()` (mounted in `dashboard-layout.tsx`) that, on an authenticated session whose stored timezone is still the bare `"UTC"` default, silently backfills the browser's real IANA zone via `PATCH /me/timezone` — never overwriting a zone the user actually chose. Extracted `getBrowserTimezone()` and added a 5-case unit test. No UI proof (silent internal backfill); verified via lint/typecheck/test (114 frontend tests) + clean convention review.
- [x] In a call, the turn off mic and camera buttons work but there is no feedback. the user doesn't know which state each of this is. — root cause: the old code put `data-[lk-enabled=false]:` Tailwind variants on the child icons, but that attribute lives on the parent LiveKit `TrackToggle`, so the icon swap never fired. New `components/call-controls.tsx` drives the UI off the real `useLocalParticipant()` state: reliable icon swap, red (destructive) button when off, and an explicit "Mic on/off" · "Camera on/off" text label under each control. Colocated unit test covers on/off/mixed states. No live UI proof — needs a connected LiveKit room and the sandbox can't reach the project's MongoDB Atlas cluster; behavior verified via the unit test + lint/typecheck/full suite + clean convention review.
- [x] en llamada: incluir timer, incluir blurry background — (1) Timer: new `lib/format-duration.ts` + `components/call-timer.tsx` show a live mm:ss / h:mm:ss call duration in the header, started once from `LiveKitRoom` `onConnected` (reconnect-safe) and ticking each second; covered by unit tests. (2) Background blur: a `BackgroundBlurToggle` in `call-controls.tsx` applies/removes a MediaPipe blur on the local camera track via `@livekit/track-processors` (`setProcessor`/`stopProcessor`), disabled while the camera is off, graceful-degrading with a toast if the model fails so the call never breaks, and synced to the real track processor so it can't desync across camera off→on. Timer verified via unit test; blur verified via typecheck + production build + unit-tested UI states — the live blur RENDERING couldn't be exercised in this sandbox (no camera / live LiveKit room / Atlas access).
- [x] que aparezca numero de conversaciones en la lista de los matches — each match candidate now carries `conversationCount` (past non-cancelled meetings); rendered on the match cards as a pluralized "{{count}} conversation" line. Committed earlier in d6dbb8d. No UI proof — sandbox can't reach the project's MongoDB Atlas cluster.
- [x] de "save circles" que vaya a dashboard (q no haya que darle a back to dashboard) — `my-circles.tsx` now `navigate("/dashboard")`s in the save mutation's onSuccess (after the cache update and "Saved" toast, which persists across the route change). The top "Dashboard" back link stays for leaving without saving. No UI proof — sandbox can't reach the project's MongoDB Atlas cluster.
- [x] Correo de confirmacion en español y arreglar horas — "arreglar horas" was already resolved (timezone backfill, f31c4e2), so emails render in each recipient's zone. For the Spanish half: the meeting/scheduling confirmation emails were already bilingual; the one remaining user-facing English-only transactional email was the password reset. New `backend/src/auth/password-reset-email.ts` holds bilingual (en/es) copy + a branded HTML shell mirroring `meeting-email.ts`, and `auth.service.ts` now sends it in `user.locale` (es → Spanish). The admin-application email is intentionally left English — it goes to an ops inbox (`ADMIN_NOTIFICATION_EMAIL`), not a user, so there's no user locale to key off. Added a unit test that a Spanish-locale user gets the Spanish reset email. No UI proof (backend email; sandbox has no SMTP/Atlas); verified via lint/typecheck + 283 backend tests + clean review.

## 2026-07-01
- [x] Links on emails are not working. We should build them using env variables so it works in prod and dev. — All backend email links (reset password, meeting join, scheduling) already used `FRONTEND_URL` correctly; the misconfiguration was a production env var issue. Fixed: (1) dashboard invite link was hardcoded `https://www.sondratalk.com` — replaced with `${window.location.origin}/signup` so it always reflects the live domain; (2) email footer links in `password-reset-email.ts` and `meeting-email.ts` were hardcoded `sondratalk.com` — now derived from `new URL(resetUrl/joinUrl).origin/hostname`; (3) updated `.env.example` FRONTEND_URL comment to document it covers all email links (reset, join, scheduling) with a warning not to use the API domain; (4) fixed the `auth.service.spec.ts` configService mock to return a valid URL for `FRONTEND_URL` so it works with `new URL()`. No UI proof — MongoDB Atlas unreachable in sandbox; verified via lint/typecheck + 283 backend + 125 frontend tests.
- [x] can we make private circles like "Hebraica" only to be shown to people that type the word in the "Add more" text field? — `CirclesService.findAllPaginated` (the no-query browse path) now adds `isPrivate: { $ne: true }` to the Mongoose filter, so private circles are invisible when users browse the catalog without typing anything. When a user types a search query, the Atlas Search path (`searchPaginated`) is used, which returns all matching circles including private ones — so typing the exact name (e.g. "Hebraica") reveals the circle. Admin view (`findAllPaginatedForAdmin`) is unchanged — admins always see all circles. Updated spec assertions to match the new filter. No UI proof — sandbox can't reach MongoDB Atlas; verified via lint/typecheck + full test suite (283 backend + 125 frontend).
- [x] I want to be able to delete or edit circles in backend; also, if private circle I want to be able to see and change the password — added Edit and Delete actions to the circles admin table. New `EditCircleDialog` component (mirrors `AddCircleDialog` but pre-fills all fields; for private circles, password field shows with "Leave blank to keep current" hint so admin can change or leave untouched). Delete uses shadcn `AlertDialog` for confirmation. Added `updateCircleApi` (PATCH) and `removeCircleApi` (DELETE) to `frontend/src/lib/circles.ts`, with spec coverage. Added 9 i18n keys in en.json/es.json. No UI proof — MongoDB Atlas unreachable in sandbox; verified via lint/typecheck + 283 backend + 127 frontend tests + clean convention review.
- [x] fix a bug; i cant add new circles now; dont know why — root cause: `EMPTY_DEFAULTS` in `add-circle-dialog.tsx` had `password: ""`. The schema uses `z.string().min(1).optional()`, which rejects `""` (empty string is not undefined and fails `.min(1)`), causing react-hook-form to block form submission silently when `isPrivate` is false and the password field is hidden. Fix: remove `password: ""` from `EMPTY_DEFAULTS` so the field defaults to `undefined`, which satisfies the `.optional()` constraint. No UI proof — MongoDB Atlas unreachable in sandbox; verified via lint/typecheck + 283 backend + 127 frontend tests.
- [x] Keep user logged in so they dont have to log in each time they enter the site. — extended `JWT_REFRESH_EXPIRATION` from 7d to 30d in `.env.example` (and local `.env`); changed session cleanup in `UsersService.addSession` from pruning by `createdAt` to `lastUsedAt`, giving a true sliding window — active users stay logged in indefinitely as long as they visit within 30 days. Updated `auth.service.spec.ts` and `users.service.spec.ts` to reflect the new TTL and field name. No UI proof (backend/internal change); verified via lint/typecheck + 283 backend + 127 frontend tests.
- [x] When a private circle is active, the user will match only with people of that same private circle — in `MatchingService.collectCandidates`, before fetching candidates, `findByIds` now checks whether any requested circles are private; if so, `poolCircleIds` narrows to those private circles only so `findOtherUserIdsInCircles` is called with that restricted set (instead of all requested circles). Ranking (`findCircleMembershipsForUsers`) and `sharedCircles` badges also use `poolCircleIds` so the displayed circles reflect only what gated the match. One `findByIds` call covers both the gating check and the display map (eliminated the old second call). Two new spec cases cover the private-gate and all-public branches. No UI proof (backend matching logic; MongoDB Atlas unreachable in sandbox); verified via lint/typecheck + 285 backend + 127 frontend tests.

## 2026-07-22
- [x] as we took out the waitlist there is no reason to restrict people to apply and sign up. Let anyone enter and archive or hide the "early access" section in the backend — removed the allowlist gate from `AuthService.signup` (no more `ForbiddenException` for non-allowlisted emails; first-user-becomes-admin bootstrap logic unchanged) and dropped the now-unused `AllowlistModule` import from `AuthModule`. Archived rather than deleted the admin "Early Access" section: removed its sidebar nav item and `/dashboard/allowlist` route so it's no longer reachable in the UI, but left the backend `AllowlistModule`/controller/service/schema and the frontend `allowlist.tsx` page + dialogs fully intact in the tree in case it needs to come back. Updated `auth.service.spec.ts` (removed the allowlist-block tests, added one covering signup succeeding regardless of allowlist status), deleted the obsolete `early-access-signup-blocked.spec.ts` e2e test, and stripped the now-pointless allowlist-the-domain setup step from `shot-dashboard-width.mjs` / `shot-font-size.mjs`. No UI proof — MongoDB Atlas unreachable in sandbox (signup/dashboard require a live DB); verified via lint/typecheck + 283 backend + 127 frontend tests + clean convention review.
