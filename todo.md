- [ ] Links on emails are not working. We should build them using env variables so it works in prod and dev.
- [ ] Time on confirmation emails is in UTC. I think this is confusing to users. We should show each user timezone on the emails.
